'use client'

// Intake multi-step form — implemented in next increment.
export default function IntakePage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-400">Intake form — coming next increment</p>
    </div>
  )
}
